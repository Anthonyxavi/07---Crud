

/* The class "user" in JavaScript defines properties for a user object including avatar, balance, first
name, gender, id, isActive, and last name. */
export class user {
    constructor({avatar,balance,firstName,gender,id,isActive,lastName,}) {
this.avatar=    avatar;
this.balance=   balance;
this.firstName= firstName;
this.gender=    gender;
this.id=        id;
this.isActive=  isActive;
this.lastName=  lastName;
    }
}